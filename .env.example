# Hostinger / Production Server Configuration
PORT=3000

# Hostinger MySQL Database Credentials (optional in dev, required in production)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306

# Note: Gemini API keys are entered directly by each user/shop owner via the UI settings and stored per shop.
